import SwiftUI

@main // This is required to mark the app's entry point
struct iosTask1App: App {
    var body: some Scene {
        WindowGroup {
            ContentView() // Your main view
        }
    }
}
